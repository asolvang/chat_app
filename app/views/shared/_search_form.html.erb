<%= form_with url: user_search_index_path, method: :get, data: { action: "ajax:success->user-search#handleResults", 'user-search-target': "form" } do |form| %>
  <div>
    <%= form.text_field :search, data: { action: "user-search#searchUsers" }, autocomplete: "off" %>
  </div>
<% end %>