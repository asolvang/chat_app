<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>

  <body>
    <div id="menu" class="flex">
      <aside class="h-screen sticky top-0  bg-blue-900 w-1/3 pl-5 pt-5">
        <%= render 'layouts/aside' %>
      </aside>
      <main class="w-full text-black pt-5 pl-5">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
